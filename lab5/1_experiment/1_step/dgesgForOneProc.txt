==> mem.bash <==
		echo "Array size: ${#array[@]}" >> report.log
	fi
done

==> mem.bash <==
		echo "Array size: ${#array[@]}" >> report.log
	fi
done

==> mem.bash <==
		echo "Array size: ${#array[@]}" >> report.log
	fi
done

==> mem.bash <==
		echo "Array size: ${#array[@]}" >> report.log
	fi
done
